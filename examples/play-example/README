#Prerequisites:

* AWS CodeDeploy Application and Deployment Group
* S3 bucket

#Usage:

    sbt
    > project playExample
    > codedeployPush
    > codedeployCreateDeployment Test

#Verify:

SSH into instance and check that a response comes back from the play server.

    curl localhost:9000